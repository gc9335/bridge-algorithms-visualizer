# 桥查找算法详解

本文档面向“实验五：桥”的实验报告，解释当前代码中的两种算法：基准算法和“生成树 + 并查集”的高效算法。高效算法没有使用 Tarjan 的 `dfn/low` 思想，而是利用并查集把已经确认在环上的树边压缩跳过。

## 1. 问题定义

在无向图中，如果删除一条边后，图的连通块数量增加，那么这条边就是桥。等价地说：

- 如果一条边在某个环上，它不是桥，因为删掉它后仍然能沿环的另一侧到达。
- 如果一条边不在任何环上，它是桥，因为它是连接两个部分的唯一通道。

实验目标是找出无向图中的全部桥。

## 2. 图结构

项目中的 `Graph` 保存三个核心信息：

- `V`：顶点数。
- `adj`：邻接表，用于 BFS/DFS 遍历。
- `edges`：无向边列表，每条边只保存一次，并保证 `u < v`。

`graph_loader.cpp` 在读入文件时会忽略自环，并通过邻接表查找去掉重复边。`countComponents()` 使用 BFS 统计当前图的连通块个数。

## 3. 基准算法：删边 + BFS

对应文件：`benchmark_bridge.cpp`

基准算法完全按照实验要求的朴素思路实现：

1. 先计算原图连通块数量 `originalComponents`。
2. 枚举每条边 `(u, v)`。
3. 从邻接表中临时删除 `(u, v)`。
4. 再次 BFS 统计连通块数量 `newComponents`。
5. 如果 `newComponents > originalComponents`，说明删除这条边让图被切开，因此 `(u, v)` 是桥。
6. 把边加回邻接表，继续测试下一条边。

这个算法的优点是非常直观，和桥的定义完全一致，适合作为正确性对照。缺点是每条边都要重新做一次全图 BFS，所以很慢。

复杂度：

- 单次连通块统计是 `O(V + E)`。
- 每条边都统计一次，因此总时间复杂度约为 `O(E * (V + E))`。
- 额外空间复杂度为 `O(V)`，主要来自 BFS 的访问数组和队列。

## 4. 高效算法：生成树 + 并查集

对应文件：`efficient_bridge.cpp`

核心思想是：先构造一棵生成森林。对于任意一条非树边 `(u, v)`，它一定会和生成树中 `u` 到 `v` 的路径形成一个环。这个环上的所有树边都不是桥。最后，没有被任何非树边形成的环覆盖到的树边，就是桥。

### 4.1 构建生成森林

算法使用显式栈遍历图，记录：

- `parent[x]`：顶点 `x` 在生成树中的父节点。
- `depth[x]`：顶点 `x` 在生成树中的深度。
- `nonTreeEdges`：遍历时遇到的非树边。

根节点没有父边，所以根节点对应的 `is_bridge[root]` 被设为 `false`。其他节点 `i` 的父边 `(parent[i], i)` 先全部假设为桥。

### 4.2 非树边为什么能排除桥

设有一条非树边 `(u, v)`。在生成树中，`u` 和 `v` 原本已经有一条唯一路径。现在再加上非树边 `(u, v)`，就得到一个环：

```text
u -> ...tree path... -> v
|                     |
+------ non-tree -----+
```

环上任意一条树边都不是桥，因为删掉其中一条边后，还可以沿环的另一侧绕过去。

所以每处理一条非树边，算法就沿生成树路径向上走，把路径上的树边标记为“非桥”。

### 4.3 并查集如何加速“向上走”

如果每条非树边都从 `u` 和 `v` 一步一步爬到最近公共祖先，最坏情况下仍然可能重复扫描很多树边。当前算法用并查集做路径压缩：

1. `uf.find(x)` 表示从 `x` 开始，向上跳过已经处理过的树边后，当前还需要检查的代表节点。
2. 处理非树边 `(u, v)` 时，令 `ru = find(u)`，`rv = find(v)`。
3. 当 `ru != rv` 时，选择深度更大的代表节点向上走。
4. 标记 `ru` 的父边 `(parent[ru], ru)` 为非桥。
5. 将 `ru` 与它的父节点合并。以后再遇到经过这条边的路径，就能直接跳过它。
6. 两侧代表节点相遇时，说明这条非树边对应的树路径已经全部处理完。

这个并查集不是用来维护图的连通块，而是用来维护“哪些树边已经被证明处于环上，可以跳过”。这正是它比朴素爬树更快的地方。

### 4.4 收集答案

处理完所有非树边后：

- 非树边一定不是桥，因为它本身就在一个环上。
- 被标记为非桥的树边也不是桥，因为它们被某条非树边形成的环覆盖。
- 剩下仍为 `true` 的树边没有被任何环覆盖，因此就是桥。

最终遍历所有顶点 `i`，如果 `is_bridge[i] == true`，就把 `(parent[i], i)` 加入答案。

## 5. 正确性说明

必要性：如果一条树边被算法保留下来，说明没有任何非树边形成的环经过它。如果删除这条树边，生成树会被切成两部分，而不存在跨过这两部分的非树边，否则算法会在处理那条非树边时标记它。因此删除它会增加连通块数量，它是桥。

充分性：如果一条边不是桥，则它一定在某个环上。若它是非树边，它天然不是桥；若它是树边，那么环上必然还存在至少一条非树边，这条非树边连接了该树边两侧的某两个顶点。处理这条非树边时，算法会沿生成树路径标记环上的树边，因此该树边会被标记为非桥。

因此，算法最后保留下来的边恰好是所有桥。

## 6. 复杂度分析

高效算法主要包括三部分：

- 构建生成森林并收集非树边：`O(V + E)`。
- 处理非树边：每条树边一旦被标记，就会被并查集合并到父节点，后续会被压缩跳过，因此总处理次数接近线性。
- 收集答案：`O(V)`。

所以总时间复杂度可写为 `O((V + E) α(V))`，其中 `α(V)` 是并查集的反阿克曼函数，实际可以看作接近常数。实验报告中也可以简化描述为“接近线性 `O(V + E)`”。

空间复杂度为 `O(V + E)`，主要来自邻接表、父节点数组、深度数组、桥标记数组、非树边数组和并查集数组。

## 7. 可视化建议

可视化时建议把高效算法拆成以下阶段：

1. 原始图：展示顶点和边。
2. 生成森林：树边变为主色，非树边暂时变淡。
3. 发现非树边：选中一条非树边 `(u, v)`。
4. 形成环：高亮生成树中 `u` 到 `v` 的路径。
5. 标记非桥：路径上的树边变为“非桥”状态。
6. 并查集合并：被处理过的节点向父节点压缩，后续路径跳过这些节点。
7. 输出桥：仍未被标记的树边用醒目颜色高亮。

这样能把“桥等价于不在任何环上”这个结论直接展示出来，也能突出并查集在算法中的作用。

## 8. 实验报告建议

报告中可以将两种算法放在同一张表中比较：

| 维度 | 基准算法 | 生成树 + 并查集算法 |
|---|---|---|
| 核心思想 | 逐条删边，重新统计连通块 | 用非树边形成的环排除非桥 |
| 是否使用 Tarjan | 否 | 否 |
| 主要数据结构 | 邻接表、BFS 队列 | 邻接表、生成树数组、并查集 |
| 时间复杂度 | `O(E * (V + E))` | 接近 `O(V + E)` |
| 适用场景 | 小图、正确性对照 | 中大型图、性能实验 |

